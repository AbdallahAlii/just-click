# Server-only (never NEXT_PUBLIC_*). Used by src/app/api/backend/[...path]/route.js
# DEV:
FLASK_ORIGIN=http://127.0.0.1:7000

# PROD (same VPS, Flask bound to localhost):
# FLASK_ORIGIN=http://127.0.0.1:7000
